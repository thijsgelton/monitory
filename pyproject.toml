[project]
name = "monitory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aider-chat[playwright]>=0.85.1",
    "beautifulsoup4>=4.13.4",
    "python-telegram-bot[job-queue]>=22.2",
    "requests>=2.32.4",
]
